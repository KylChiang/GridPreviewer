# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GridPreviewer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GridPreviewer
	pod 'Alamofire', '5.1.0'

  target 'GridPreviewerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GridPreviewerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
